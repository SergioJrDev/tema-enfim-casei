a, button {
	cursor: pointer;
}

a {
	color: var(--color_btn);
} 

.disabled {
	pointer-events: none;
	background: red;
}

.btn {
	color: var(--color_btn);
	display: inline-block;
	position: relative;
	border: 0;
	/*padding: 10px 0;*/
	&-small {
		padding: 5px;
	}
	&-theme {
		background: var(--color_btn);
		color: #fff;
		padding: 10px;
	}
	&-theme-invert {
		color: var(--color_btn);
		background: #fff;
		font-weight: bold;
		text-transform: uppercase;
	}
	&-radius {
		borderRadius(3px);
	}
	&-shadow {
		box-shadow: 4px 4px var(--color_btn)_dark;
	}
	&-danger {
	    background: #ff6d6d;
	    padding: 3px 8px;
	    border-radius: 4px;
	    color: #fff;
	}
	&-gray {
		background: #565656;
		padding: 3px 8px;
		border-radius: 4px;
		color: #fff;
	}
	&-block {
		width: 100%;
		display: block;
		text-align: center;
	}
	&-circle {
		borderRadius(50%);
	}
	&-dark {
		background: #454545;
	}
	&-pink {
		background: var(--color_demo_theme);
	}
	&-uppercase {
		text-transform: uppercase;
	}
	&-small {
		padding: 5px;
		font-size: 14px;
	}
}
